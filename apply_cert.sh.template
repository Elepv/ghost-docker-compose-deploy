#!/bin/bash
# 申请证书
~/.acme.sh/acme.sh --issue -d ENV_DOMAIN_NAME --standalone -k ec-256 --force --insecure

# 安装证书
~/.acme.sh/acme.sh --install-cert -d ENV_DOMAIN_NAME \
    --key-file ~/certs/ENV_DOMAIN_NAME.key \
    --fullchain-file ~/certs/fullchain.cer